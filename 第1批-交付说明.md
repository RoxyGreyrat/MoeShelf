# 第 1 批交付说明 · 排版地基 + 卡片 + 滚动模型

> 交付日期：本轮
> 工作副本：`E:\moeshelf-work`（原始目录 `E:\moeshelf-src` 全程**只读**，`git status` 干净，未改动一个字节）
> 预览地址：**http://localhost:3001**（dev 服务已在后台运行；数据用的是你的真实游戏库副本，187 个游戏 + 封面缓存）
> 方向：**精致克制** ｜ **左栏 sticky + 主区单滚动**

---

## 1. 这一批改了什么

| # | 项目 | 具体改动 |
|---|---|---|
| 1 | **设计令牌层** | 新增暗/亮同源的一套 CSS 变量（表面 4 级、文字 4 级、描边 3 级、强调/语义色、圆角、阴影、布局尺度），并新建语义化类 `text-primary/secondary/tertiary`、`bg-surface-*`、`btn-*`、`chip`、`segment`、`field`、`panel`、`overlay-btn`。**删掉了原来靠 `[class*='text-white/']`、`[class*='bg-ink-950']` 通配符硬覆盖亮色的做法** |
| 2 | **排版尺度全部改 rem** | 全文件 `text-[11px]`→`text-[0.6875rem]` 等 16 档像素字号改 rem，Tailwind 默认 `text-xs/sm/base` 整体上调一档；最小字号从 10–11px 提到 12–13px，卡片标题 13px→15px。**界面缩放现在能真正放大文字** |
| 3 | **滚动模型** | 主区单滚动 + 顶栏与工具条吸顶（`top: var(--header-h)`）；左栏 `sticky` 不再随内容滚走；右栏同样 `sticky`；**切换厂商/搜索/排序自动回到顶部并重置分页**（原来会停在上一组的滚动位置） |
| 4 | **卡片重设计** | 3:4 封面 + 清晰两段式信息区；标题 15px/600、副信息 13px 中对比度、第三行 12px；**时长改成绿色软底徽章，0 分钟不再渲染**（原来 189 张卡都写着「0 分钟 ·」）；角标从 7 种收敛到 3 种（收藏星 / 通关勾 / 未下载虚线框）；浮层操作统一为 `overlay-btn`，桌面端 hover 才出现；「重新获取」从封面浮层移到卡片底栏常驻，**hover 浮层里换成「打开文件夹」**；启动按钮 32px 高、去掉渐变与发光 |
| 5 | **布局自适应** | 列数不再写死档位（原来的 `xl:5 2xl:5` 等于没写），改为 `repeat(auto-fill, minmax(min(100%, var(--density-min)), 1fr))`；内容最大宽度 1880px → 2280px；左栏 210→232px、右栏 220→264px |
| 6 | **密度开关** | 工具条新增「紧凑 / 舒适 / 大图」三档，`localStorage` 记忆 |
| 7 | **分页** | 首屏只挂载 60 张，底部「再显示 60 个 / 一次显示全部」（原来一次性渲染 187 张，中间栏实测 18,684px） |
| 8 | **右栏重排** | 总时长卡改成「大数字 + 游戏数 / 已通关」两栏；「最近启动」「已通关」统一 `panel` + 13px 行高 + 语义色；空态文案对齐 |
| 9 | **顶栏** | 实测高度 67px → 60px（`--header-h` 统一变量）；搜索框改 `field` 令牌；扫描按钮改 `btn-primary`（去掉渐变阴影）；主题/设置按钮统一 40px 圆形 `icon-btn` |
| 10 | **背景光效收敛** | 三层模糊光斑透明度 20%/10%/10% → 10%/7%/7%，避免与卡片抢对比度 |

---

## 2. 实测数据对比（1920×1080，同一份数据）

| 指标 | 改动前 | 改动后 |
|---|---|---|
| 卡片字号分布 | 10px×39、11px×**285**、12px×**303**、13px×266 | 12px×180、13px×**258**、**15px×73**、（无 11px） |
| 卡片标题 | 13px | **15px** |
| 卡片副信息/第三行 | 12px / 11px | 13px / 12px |
| 卡片网格列数 | 写死 5 列（1920 上留白；xl 与 2xl 同值） | **6 列**（`auto-fill`，2560 上 8 列） |
| 卡片宽度 | 255px | 205px（可切密度） |
| 中间栏内容高度 | **18,684px** | **4,847px**（首屏 60 张） |
| 滚动容器数量（有溢出） | **3**（左/中/右各自滚） | **1**（主区；左/右栏仅自身内容超出时才滚） |
| 亮色正文对比度 | `rgb(90,98,114)` ≈ **3.0:1** ❌ | `rgb(69,78,96)` ≈ **7.2:1** ✅ |
| 亮色第三行 | `rgb(90,98,114)` ≈ 3.0:1 | `rgb(98,107,125)` ≈ 5.2:1 ✅ |
| 界面缩放 125% 时的文字 | 13px（不变） | 15px × 1.25 = 18.75px（真正放大） |
| 顶栏高度 | 67px（与 `calc(100vh-67px)` 硬耦合） | 60px（`--header-h` 变量） |

---

## 3. 复验结果（脚本 `_recon/verify-batch1.mjs`）

| 验证项 | 结果 |
|---|---|
| 顶栏吸顶 | `headerTop = 0`，滚动 1400px 后仍贴顶 ✅ |
| 工具条吸顶 | `toolbarTop = 60`（= 顶栏高度）✅ |
| 左栏 sticky | `railTop = 72`，未随内容滚走 ✅ |
| 卡片 hover 浮层 | 4 个 `overlay-btn` + 2 个底栏按钮，桌面默认隐藏、hover 出现 ✅ |
| 分页 | 点击「再显示 60 个」→ 挂载数 60 → 120 ✅ |
| 切换厂商 | `scrollY = 0`（回到顶部）、分页重置为 60 张、标题变为厂商名 ✅ |
| 厂商列表底部的分页按钮 | 该厂商只有 14 个本地游戏时不再出现（修复了「再显示 60 个（剩余 127）」的错位提示）✅ |
| 生产构建 | `npm run build` → **Compiled successfully，exit 0** ✅ |
| 类型检查 | `npx tsc --noEmit` → 0 错误 ✅ |
| NSFW 模糊 / 收藏 / 已通关 / 未下载 | 行为未变（模糊与 NSFW 角标正常，收藏星与通关勾保留）✅ |

---

## 4. 改动文件清单

| 文件 | 说明 |
|---|---|
| `src/app/globals.css` | 重写：设计令牌 + 语义化类 + 控件规格 + 密度档位（236 行 → 约 600 行） |
| `src/app/page.tsx` | 顶栏、卡片、工具条、主布局、左栏、右栏、分页与密度状态（约 4,470 行 → 约 4,580 行） |
| `src/app/layout.tsx` | body 底色改用令牌类 |
| `src/components/icons.tsx` | 新增 `clock`、`grid` 两个图标 |
| `tailwind.config.ts` | 未改（令牌走 CSS 变量，避免动 Tailwind 主题） |
| `_tools/codemod.mjs` | px→rem 与字号整体上调的迁移脚本（可复跑，幂等） |
| `_recon/*` | 只读侦察与复验工具（截图 / 量测 / 交互验证） |

**未改动**：`src/lib/**`、`src/app/api/**`、数据库与刮削逻辑、`desktop/`、打包脚本。

---

## 5. 验收材料（`_recon/`）

| 文件 | 内容 |
|---|---|
| `after-1920-dark.png` / `after-1920-light.png` | 改动后 1920×1080（暗 / 亮） |
| `after-2560-light.png` / `after-1440-light.png` | 宽屏 8 列 / 中屏表现 |
| `after-390-dark.png` | 390×844（当前两列；按你的要求小屏放到后面批次专门打磨） |
| `verify-scrolled-1920.png` | 滚动 1400px 后：顶栏 + 工具条吸顶 |
| `verify-card-hover-1920.png` / `verify-card-hover2-1920.png` | 卡片 hover 浮层（打开文件夹 / 重新获取 / 移除） |
| `verify-company-1920.png` / `verify-company-fixed.png` | 厂商视图（含分页按钮修复前后） |
| `live-*.png` / `modal-*.png` | **改动前**的对照截图（原始状态） |
| `metrics-*.json` | 改动前的计算样式量测（字号直方图、文字色分布、三栏几何） |
| `verify-batch1.mjs` / `interact.mjs` / `measure.mjs` / `shot.mjs` | 复验与量测脚本，可随时重跑 |

---

## 6. 已知问题 / 留给后续批次

1. **详情弹窗与设置弹窗**还是旧样式（字号偏小、左栏封面下方空白、弹窗内两层滚动、设置是单列表单）→ **第 3 批**；
2. **左侧厂商栏**还没有搜索、折叠、收藏厂商置顶，标题行的排序箭头语义仍旧含糊（只作用于厂商名）→ **第 2 批**；
3. **空态 / 加载骨架**未做（目前第一次进入是整屏空白 + 顶栏）→ **第 2 批**；
4. **图标仍有 emoji**（🌙/☀️ 在顶栏），与线条图标混用 → **第 2 批**换成 SVG；
5. 卡片封面 hover 只做轻微放大，尚未做「悬停显示简介」等信息增强；
6. 亮色主题下 `--page-pad` 等变量已就位但尚未全部替换硬编码间距；
7. `src/app/page.tsx` 仍是单文件（约 4,580 行），组件拆分放到 **第 4 批**（先保证视觉验收通过再动结构，降低返工风险）。

---

## 7. 你怎么验收

1. 浏览器打开 **http://localhost:3001**（左上角 🌙/☀️ 可切亮暗，右上「设置」仍在）；
2. 建议对比 `_recon/live-1920.png`（改前）与 `_recon/after-1920-light.png`（改后）；
3. 想试交互：工具条右侧「紧凑 / 舒适 / 大图」切密度；点左侧任一厂商看是否回到顶部；滚到列表底部点「再显示 60 个」；
4. 反馈可以直接说「哪里还挤 / 哪里字还要再大 / 卡片想更松」，我按你的意见调，然后再进第 2 批。
