# 第 4 批交付说明 · 移动端打磨 + 组件拆分

> 工作副本：`E:\moeshelf-work`（原始目录 `E:\moeshelf-src` 始终只读，`git status` 干净）
> 预览地址：**http://localhost:3001**
> 前置：第 1 批（排版地基/卡片/滚动）、第 2 批（左导航/右栏/骨架）、第 3 批（两个弹窗）已完成

---

## 1. 移动端打磨（390 × 844 实测）

| 项目 | 改动前 | 改动后 |
|---|---|---|
| **厂商筛选** | 一整排 **189 个 chip** 横滑，无入口提示 | 首行只留 4 个：`全部 187 / 当前厂商 / ★ 收藏 / 厂商 · 76`；点「厂商 · 76」在下方展开可滚的完整列表（限高 160px），选完自动收起 |
| **卡片操作** | 一排 3 个黑色圆钮常驻压在封面上（桌面才是 hover 出现） | 收进 **`…` 菜单**：查看详情 / 打开文件夹 / 重新获取信息 / 从列表移除；封面只剩收藏星标 |
| **回到顶部** | 无 | 滚动超过 800px 出现右下浮动按钮，点击平滑回顶（实测 `scrollY 1500 → 0`） |
| 网格 | 1 列（最小卡宽 184px 在 390px 上放不下两列） | **2 列**（173px 卡宽），文档高度 38,129 → 12,205px |

实测：`CHIP_ROW = ["全部 187","厂商 · 76"]`（收起态）；`MENU_ITEMS = ["查看详情","打开文件夹","重新获取信息","从列表移除"]`；备份按钮 `visible: true, y: 780`。

---

## 2. 组件拆分

`page.tsx` **5,113 行 → 2,612 行（-49%）**，拆出的模块：

| 文件 | 行数 | 内容 |
|---|---|---|
| `src/lib/ui-shared.ts` | 380 | 共享类型（`Game` / `GameMetadata` / `CharacterEntry` / `SearchCandidate` …）、常量（`SOURCE_LABELS` / `PAGE_STEP` / `DENSITY_LABEL` / `ACT_GROUPS`）、工具函数（`displayTitle` / `formatRelease` / `formatDuration` / `relativeTime` / `normalizeDev` / `matchesFilter` / `colorFor` / `idbGet` / `idbSave` …）、刮削开关存取器 |
| `src/components/ui/primitives.tsx` | 228 | `CoverPlaceholder` / `CoverImage` / `ClockIcon` / `SearchCandidateRow` / `SectionRow` |
| `src/components/detail/GameDetailModal.tsx` | 1,227 | 详情弹窗（含「高级设置」与管理面板） |
| `src/components/settings/SettingsModal.tsx` | 588 | 全局设置弹窗 |
| `src/components/settings/DirPicker.tsx` | 192 | 目录选择器 |
| `src/app/page.tsx` | 2,612 | 页面容器：顶栏、卡片、工具条、左栏、右栏、数据编排 |

做法：用脚本按 `function X(` 边界整段搬移（**不重写任何 className 与逻辑**），逐个补 import 后再跑类型检查与行为复验。`nsfwBlur` 从「导出的可变变量」改成 `isNsfwBlurEnabled() / setNsfwBlurEnabled()`（ESM 导入绑定不可重新赋值），语义不变。

---

## 3. 复验结果（拆分后全量重跑）

| 批次 | 验证项 | 结果 |
|---|---|---|
| 第 1 批 | 顶栏/工具条吸顶 | `headerTop=0, toolbarTop=60, railTop=72` ✅ |
| 第 1 批 | 分页 | 60 → 120 ✅；切厂商 `scrollY=0` ✅ |
| 第 2 批 | 左栏展开 | 侧栏宽度 232px、按钮 97 个 ✅ |
| 第 2 批 | 厂商跳转 | `scrollY=0`、标题 `YuzuSoft`、60 张卡 ✅ |
| 第 3 批 | 详情弹窗 | 顶栏含「启动 / 打开 / 更换」、弹窗内滚动容器 **0**、角色页 10 张卡 ✅ |
| 第 3 批 | 高级设置 | 分组导航 7 项、4 个分组标题、切换生效、关闭链路正常 ✅ |
| 移动端 | 筛选/菜单/回顶 | 全部通过 ✅ |
| 构建 | `npm run build` | **Compiled successfully，exit 0**（首页 42.9 kB / First Load 130 kB）✅ |
| 类型 | `npx tsc --noEmit` | 0 错误 ✅ |

---

## 4. 验收材料（`_recon/`）

| 文件 | 说明 |
|---|---|
| `mobile-v2.png` | 移动端主界面（两列 + 收起态筛选 + `…` 菜单） |
| `mobile-menu.png` | 移动端 `…` 菜单展开 |
| `mobile-backtop.png` | 移动端「回到顶部」浮动按钮 |
| `live-mobile.png` | **改动前**的移动端（一排黑圆钮 + 189 个 chip） |
| `after-split-1920.png` | 拆分后桌面端（确认外观与拆分前一致） |
| 其余 | 第 1–3 批的对比截图与量测 JSON 均保留 |

---

## 5. 四批总览

| 批次 | 主题 | 关键收益（实测） |
|---|---|---|
| 1 | 设计令牌 + rem 排版 + 滚动模型 + 卡片 | 字号 11px 归零、标题 13→15px；中间栏高度 18,684→4,847px；滚动容器 3→1；亮色正文对比度 3.0:1→7.2:1 |
| 2 | 左导航 + 右栏 + 空态骨架 | 厂商搜索、折叠成 52px 图标条（卡片区 +180px）、骨架屏 88 元素、右栏两块列表合并 |
| 3 | 详情弹窗 + 设置弹窗 | 弹窗内滚动 0、主操作上顶栏、左栏 400px 空白填满；设置改左导航 + 右内容，15 处按钮 / 8 处输入框改令牌（修掉亮色深底深字） |
| 4 | 移动端 + 组件拆分 | 移动端 1 列→2 列、189→4 个 chip、操作进 `…` 菜单、回到顶部；`page.tsx` 5,113→2,612 行 |

全部四批的行为复验脚本都在 `_recon/verify-*.mjs`，可随时重跑。
