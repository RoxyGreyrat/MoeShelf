# Moeshelf 排版与界面优化方案

> 侦察对象：`E:\moeshelf-src`（原始文件全程**只读**，未改动任何字节）
> 实测方式：对正在运行的 `http://localhost:3000` 做无头浏览器截图 + 计算样式/几何量测（1920×1080、2560×1440、1440×900、390×844，暗色/亮色各一轮）
> 证据目录：`_recon/`（原始截图与量测 JSON 均保留）
> 版本：v1.6.1 · 本文件只是**方案**，代码改动会在 `E:\moeshelf-work` 里另起一份副本进行

---

## 0. 一句话结论

界面不难看，但**「信息密度高 + 字号过小 + 三处独立滚动 + 层次全靠透明度」**四件事叠加，让它看起来"挤、糊、找不到重点"。
其中有三条是**功能性缺陷**（不只是审美），优化收益最大：

1. **界面缩放只放大间距、不放大文字** —— 字号体系是 px，间距是 rem；
2. **亮色主题下的正文行内文字对比度约 1.35:1**（远低于 WCAG 4.5:1），实测糊成一片；
3. **189 个游戏没有分页/虚拟滚动**，中间栏实测 18,684px 高，左侧厂商栏与中间栏各自滚动、切换厂商时主区滚动位置不重置。

---

## 1. 实测到的排版问题（按严重度排序）

### P0-1 字体全部是固定 px，界面缩放对它无效（架构性）

`page.tsx:2342` / `page.tsx:3801` 用 `document.documentElement.style.fontSize = '125%'` 实现缩放，但页面字号几乎全是 Tailwind 的 `text-[11px]` / `text-xs` / `text-sm`（px，不随根字号变），只有间距、圆角、卡片宽度是 rem。

实测（1920×1080，同一页面分别设 100% / 125% / 150%）：

| 界面缩放 | 根字号 | 卡片标题 | 侧栏条目 | 卡片内边距 | 卡片宽度 | 顶栏高度 |
|---|---|---|---|---|---|---|
| 100% | 16px | **13px** | **13px** | 8px 12px | 255px | 67px |
| 125% | 20px | **13px** | **13px** | 10px 15px | 246px | 83px |
| 150% | 24px | **13px** | **13px** | 12px 18px | 237px | 99px |

→ 4K 屏用户把缩放调到 150%，得到的是"**留白更大、字一样小、封面更小**"。这正好解释了"排版不好看"的主观感受：整页的比例关系被拆散了。

**修法**：字号体系改为 rem（`text-[0.8125rem]` 一类），并把基线整体上调；`uiScale` 改为真正等比缩放（根字号 + 少量关键尺寸），或直接改用 `zoom` / `transform: scale` 语义。

### P0-2 亮色主题的正文对比度不达标（实测 1.35:1）

亮色是用 `globals.css:29-95` 的 `[class*='bg-ink-950']`、`[class*='text-white/']` 属性通配选择器硬覆盖出来的。实测亮色下页面文字颜色分布：

| 颜色 | 出现次数 | 用途 | 相对底色 `#f2f4f8` 对比度 |
|---|---|---|---|
| `rgb(90,98,114)` | **273** | 卡片副信息（发售日/开发商/时长/路径） | **≈ 3.0 : 1** ❌ |
| `rgb(77,85,102)` | 192 | 卡片第二行 | ≈ 3.9 : 1 ⚠️ |
| `rgb(35,40,51)` | 190 | 标题 | ≈ 11 : 1 ✅ |

而暗色下同一处文字是 `rgba(255,255,255,0.25)`（≈ **1.35:1**）与 `rgba(255,255,255,0.4)`（≈ 2.4:1）——**都不是给正文用的透明度**。
`fontSizeHistogram` 实测：10px×39、11px×**285**、12px×**303**、13px×**266**，即**99% 的文字落在 10–13px**，全页只有 2 处 16px 以上。小字号 + 低对比度 = 视觉上"糊"。

**修法**：建立语义化色彩层（`--text-primary/secondary/tertiary`、`--surface-1/2/3`、`--border-subtle`），把暗/亮两套值都定义在 `:root[data-theme]` 上，删掉属性通配 hack；副信息最低 3.5:1，正文 ≥ 4.5:1。

### P0-3 滚动模型：三处独立滚动 + 189 张卡全量渲染

实测 1920×1080：

```
main       1880×1013  overflow: hidden  (lg:h-[calc(100vh-67px)])
├─ aside#1   210×989  overflow-y: auto   ← 左侧厂商栏（自己滚）
├─ section  1354×989  overflow-y: auto   ← 中间卡片区（自己滚），内部 grid 高 18684px
└─ aside#2   220×989  overflow-y: auto   ← 右侧统计栏（自己滚）
scrollContainersWithOverflow = 3
```

问题：
- 鼠标停在侧栏滚的是侧栏，停在中间滚的是中间，**"滚动失灵"感**很强；
- 中间栏一屏只能看到约 2 行卡片（5 列 × 2 行），189 个游戏要看 **38 行**；
- 左侧厂商栏 189 个条目 × 32px ≈ 6000px，塞在 210px 宽的小面板里，两侧各一条细滚动条；
- 切换厂商后中间栏**不回到顶部**，会停在上一组的滚动位置；
- `min-h-screen` 与 `lg:h-[calc(100vh-67px)]` 并存，窗口高度变化时有 67px 的脆弱耦合。

**修法**：改为「**左侧栏 sticky + 主区单一滚动**」；主区顶部吸顶工具条（排序/筛选/结果数）；切换分组时 `scrollTo(0)`；卡片列表加分页或虚拟滚动（189 → 60/页 是巨大改善）。

### P1-4 网格列数在宽屏下浪费空间

实测 2560×1440（暗色截图 `live-2560.png`）：内容仍被 `max-w-[1880px]` 卡住，两侧各约 340px 空白；卡片网格 5 列 → 4K 上会出现 **7 列都放得下却只排 5 列**的巨幅留白。

当前档位：`grid-cols-2 sm:3 md:4 xl:5 2xl:5`（注意 **xl 与 2xl 都是 5 列**，2xl 那一档等于没写）。
建议改为按可用宽度自适应：`auto-fill minmax(clamp(180px,13vw,260px),1fr)`，并给一个「紧凑 / 舒适 / 大图」三档密度开关（记住选择）。

### P1-5 卡片自身的排版与层级

- 封面与文字区**没有层次分隔**：封面下沿只有一条 6% 的透明渐变，标题 13px/600 与副信息 12px、11px 差距太小，三行文字挤在 8px 12px 的内边距里；
- 卡片有 **3 种圆角**（卡片 `rounded-2xl`、按钮 `rounded-lg`、图标按钮 `rounded-lg/full`）、**4 种黑底叠层透明度**（`bg-black/55`、`bg-black/50`、`bg-black/65`、`bg-slate-900/40`），视觉上"毛边"；
- 收藏星标、已通关勾、刷新、删除、NSFW 眼睛、未下载、错误、暂无封面 —— **最多 7 个角标/按钮压在封面四角**（见移动端截图 `live-mobile.png`：两张卡上各露出 3 个黑色圆钮），封面是主体，却最乱；
- 「启动」按钮实高 **28px**、字号 12px，绿色实心 + 阴影，是卡片里最重的元素，**权重压过了封面**；
- 移动端卡片操作按钮 `opacity-100`（桌面才是 hover 出现），移动端一条 2 列网格上常驻一排黑圆钮；
- 实测所有卡片第三行都渲染成 `0 分钟 · 文件夹名`（`formatDuration(0)` 未做零值判断），**189 张卡上都有这个无意义前缀**；
- 「未下载」卡是灰化 + `未下载 · VNDB #xxx` 小字，与正常卡几乎难以区分，分节标题却只是两根细线夹一行 11px 文字。

**修法**：标题 15px/600 行高收紧；副信息一行 12.5px 中对比度；时长做成有色的"徽章"，0 分钟不显示；操作按钮收进 **封面 hover 浮层**（移动端改为长按/`…` 菜单）；未下载卡用虚线边框 + 彩带；把所有角标统一到一种样式规格。

### P1-6 左侧厂商栏：210px 装不下信息

实测条目高 32px、字号 13px、右侧计数 10px `1/17`；标题行里「厂商」+「获取中 22/23」+「名称 / 排序箭头」三组元素挤在一行（`page.tsx:4144-4164`），计数与排序箭头语义不清（排序箭头只作用于**厂商名**，不作用于游戏）。189 个厂商没有搜索、没有"我收藏过最多"的快捷入口。

### P1-7 右栏三张卡：层级与信息取舍

220px 宽，三块：总时长（24px 数字，是**全页最大字号**，比任何游戏标题都大）、最近启动（5 条）、已通关（32 个条目、每条 8px 缩略图）。
- 「最近启动」每条右侧只有 10px 相对时间 + 一个 12px 播放钮，行高 30px、字号 12px，挤；
- 「已通关 32」列表占了 331px 高、可滚，而它信息量最低（只是索引）；
- 三块卡各自 `space-y-3`，最大高度都吃 `max-h`，在 1080p 上刚好卡满，1440p 上留出大片空白；
- 空数据时只显示一行 12px 灰字，整块像坏了。

### P1-8 宽屏/中屏两端都不讨好

- `1440×900` 实测（`live-1440.png`）：右栏还在（`xl` = 1280px 起），中间只剩 4 列 ≈ 1000px，卡片缩到 ~230px，右栏 220px 显得局促；
- `1024–1279px`：右栏**整块消失**，信息无处可去，也没有任何提示或折叠入口；
- 顶部工具条（排序/仅收藏）在最上方，而「厂商」筛选标题在下方 20px 处，**两行紧挨的两个控件区互不相关却相邻**。

### P2-9 详情弹窗：留白与信息流

实测 `modal-detail.png`（1600×1000）：
- 左栏固定 288px，封面按原始比例显示（`fit="natural"`），下方 400+px 是**空白背景**；
- 评分/厂商/发售日三枚小标签被压到左栏最底部，与封面之间隔着一大片黑；
- 右栏上部：标题 20px + 「已通关」「VNDB」标签 + 时长徽章 + 官方中文标题 + 原名，之后是「简介」独立滚动框（`min-h-[8rem] flex-1 overflow-y-auto`），**再往下是角色横滑条**——于是弹窗内又有 2 个滚动区；
- 「设置」与「关闭」两个圆钮在右上角，主操作（启动/打开文件夹）藏在设置面板里；
- 缺少：相似作品、截图、标签、游戏时长趋势等"详情页该有的东西"。

### P2-10 设置弹窗：字段式表单，缺分组导航

实测 `modal-settings.png`：宽 640px 单列、无左侧导航、无搜索；每项 = 标签 + 只读输入框 + 一行 10-11px 说明 + 一个紫色小按钮（「保存并重启」「更改并迁移」「保存」「测试连接」四种文案混在同一列）；「界面缩放」100/112/125/150 四个按钮是最显眼的控件，但它其实是个次要设置。亮度、危险操作（清空缓存/迁移数据）与普通设置同级。

### P2-11 移动端

实测 `live-mobile.png`（390×844，文档高 **36,456px**）：
- 顶栏折成 3 行（brand / 扫描+主题+设置 / 搜索）占 160px；
- 厂商筛选是一整排横向 chip，**189 个 chip**、无「更多」入口、无吸顶；
- 卡片操作钮常驻；
- 32 个已通关、总时长等右栏信息在移动端**完全看不到**；
- 游戏数多时没有任何"回到顶部"或快速跳转。

### P3-12 工程结构（影响后续可维护性，也影响迭代速度）

- `src/app/page.tsx` = **4470 行 / 190KB**，含 15+ 组件：Header、GameCard、GameDetailModal(≈750 行)、DirPicker、SettingsModal(≈280 行)、EmptyState、Toast、SidebarItem + 主页面；
- 大量内联 `style={{...}}`（星标、NSFW 徽标、仅收藏按钮等），同一视觉语言被复制 3 份；
- 亮色主题靠属性通配符覆盖（`[class*='from-indigo'][class*='text-white']`），新增颜色几乎必然漏改；
- 无 `src/components/` 下的页面级拆分，UI 改动风险高。

---

## 2. 优化方案

### 2.1 设计令牌（Design Token）——先把"地基"换成变量

新增 `src/styles/tokens.css`，两套主题同源，删掉属性通配 hack：

```
表面：--surface-0 #0a0b10 / --surface-1 #12141d / --surface-2 #171923 / --surface-3 rgba(255,255,255,.06)
文字：--text-1 (主) --text-2 (次) --text-3 (弱) --text-muted(仅装饰)
强调：--accent #6366f1  --accent-soft  --success #10b981  --warn #f59e0b  --danger #ef4444
状态：--ring(焦点环) --shadow-card --shadow-pop
```
亮色主题用同一组变量名换值（浅灰底 + 深灰字 + 12% 边线），保证 `text-[11px] text-white/25` 这类写法**从源头消失**。

### 2.2 排版尺度（Type Scale）——全部 rem，整体上调一档

| 角色 | 现在 | 建议 | 说明 |
|---|---|---|---|
| 页面标题 / 品牌 | 18px | 1.125rem | 不变 |
| 区块标题 | 11px uppercase | 0.8125rem + 字重 600 | 不再用 10px |
| **卡片标题** | 13px | **0.9375rem / 600 / 1.35** | 两行截断 |
| 副信息 | 12px `white/40` | 0.8125rem `--text-2` | ≥ 3.5:1 |
| 元信息 / 徽章 | 11px / 10px | 0.75rem | 不再低于 12px |
| 正文（简介） | 14px `white/65` | 0.875rem `--text-1` / 行高 1.7 | 中文行高要松 |
| 数字统计 | 24px | 1.75rem / tabular-nums | 保留重点 |
| 触控目标 | 28px | ≥ 32px（移动 ≥ 40px） | 无障碍 |

### 2.3 布局与滚动模型

```
┌──────────────────────────────────────────────────────────────┐
│ 顶栏（吸顶，56-64px，固定高度）：品牌 · 搜索 · 密度 · 扫描 · 主题 · 设置 │
├────────────┬────────────────────────────────────┬────────────┤
│ 左侧导航    │ 吸顶工具条：结果数 · 排序 · 仅收藏 · 分页/密度 │ 右栏（可选）│
│ 220-240px  │                                           │ 260px      │
│ sticky     │ 卡片瀑布（单一滚动，由整个主区承担）           │ sticky     │
│ 收藏/全部   │                                           │ 统计/最近   │
│ 已通关      │ ── 未下载（虚线分节）──                    │ 已通关索引  │
│ 厂商（可搜索）│                                           │            │
│ 收起成图标   │ 分页：每页 60 / 120 / 全部                  │            │
└────────────┴────────────────────────────────────┴────────────┘
```

- 主区滚动 + 侧栏 sticky（`position: sticky; top: …; max-height: 100vh - 顶栏; overflow: auto`），**只有主区一条滚动条**；
- 切换厂商/收藏/搜索时主区 `scrollTo(0)`；
- 左侧导航加**折叠按钮**（图标态 64px），厂商列表加搜索框 + 收藏厂商置顶；1024–1279px 把右栏折叠成顶栏按钮 + 抽屉，而不是直接 `hidden`；
- 卡片列数：`repeat(auto-fill, minmax(clamp(11rem, 12vw, 16rem), 1fr))`，配 3 档密度；内容最大宽度放宽到 2200px 并居中（4K 不再两侧留 340px）。

### 2.4 卡片重设计（收益最直观的一块）

```
┌────────────────────┐
│  ⭐(左上,仅收藏时)  │  封面 3:4
│                    │  ・hover 时封面轻微放大 + 底部浮出操作层
│      封面           │  ・操作：重新获取 / 打开文件夹 / 移除（毛玻璃胶囊，右下）
│          [▶][📁][🗑]│  ・已通关：右上角金色勾（一种样式）
│  ▸ 9nine · 时长徽章 │  ・未下载：整卡虚线边框 + 左上彩带
├────────────────────┤
│ 9-nine- 天色天歌天籁音 │  0.9375rem/600，两行
│ 2018-04-27 · Palette │  0.8125rem，--text-2
│ ⏱ 12.5 小时  (无则不显示)│  强调色徽章（0 分钟不渲染）
├────────────────────┤
│ ▶ 启动          📁   │  主按钮 32-36px 高，右侧一个次级图标钮
└────────────────────┘
```
要点：角标从 7 种收敛到 3 种（收藏/通关/未下载）；操作按钮只在 hover/聚焦出现（移动端进 `…` 菜单）；字号/对比度按 2.2 提升；`0 分钟` 不再渲染。

### 2.5 详情弹窗重构

- 左栏 **sticky**：大封面 + 标题 + 关键元数据（评分/厂商/发售日/时长/来源）+ 主操作（启动、打开文件夹、重新获取），消灭底部空白；
- 右栏标签页：**简介 / 角色与声优 / 截图 / 相似作品**，只在标签内容区滚动（弹窗内不再套两层滚动）；
- 顶部统一：标题、来源徽章、已通关切换、关闭；「设置」改为次级入口（不抢视觉）；
- 宽屏下弹窗 `max-w-6xl`，窄屏改全屏 sheet（移动端从下往上）。

### 2.6 设置弹窗重构

左侧分组导航（游戏目录 / 数据存储 / 内容分级 / 外观 / 网络与代理 / 危险操作），右侧内容区；说明文字折叠为「?」气泡；按钮统一为「主按钮 + 次按钮」两档；危险操作独立分组并用 danger 色；「界面缩放」并入外观分组，并与 2.2 的 rem 体系联动（这是修复 P0-1 的地方）。

### 2.7 移动端

- 顶栏 2 行：品牌 + 搜索 + `⋮`（扫描/主题/设置/筛选收进菜单）；
- 筛选改为底部抽屉 sheet，chip 只显示"当前 + 全部收藏 + 厂商（更多）"；
- 卡片 2 列（≥ 480px 3 列），操作进长按/`…`；
- 补「回到顶部」悬浮钮；右栏统计改为可展开的顶部概览条。

### 2.8 工程重构（为后续迭代降险）

```
src/app/page.tsx            主容器 + 数据编排（目标 < 600 行）
src/components/shell/       Header、Sidebar、SidebarSection、Toolbar、DensitySwitch
src/components/game/        GameCard、GameGrid、CardBadges、CardActions、Pagination
src/components/detail/      GameDetailModal、TabBar、CharacterStrip、SimilarGames
src/components/settings/    SettingsModal、SettingsSection×6、DirPicker
src/components/ui/          Button、IconButton、Badge、Chip、Tabs、Sheet、Skeleton、EmptyState
src/styles/tokens.css       设计令牌（暗/亮同源）
```
**不改动**：`src/lib/**`、`src/app/api/**`、数据库与刮削逻辑、`desktop/`、打包脚本（本轮只碰 UI 层，行为保持一致）。

---

## 3. 实施计划（分批，每批先给成果验收）

| 批次 | 内容 | 交付物 |
|---|---|---|
| **第 1 批** | 设计令牌 + 排版尺度 + 布局/滚动模型 + 卡片重设计（含 0 分钟修复、密度开关、分页） | 可运行的 `:3001` 预览 + 前后对比截图（1920 暗/亮、2560、1440、移动） |
| **第 2 批** | 左侧导航（搜索/折叠/分组）+ 右栏重排 + 工具条吸顶 + 空态/加载骨架 | 同上 + 交互录屏式截图序列 |
| **第 3 批** | 详情弹窗重构 + 设置弹窗重构 | 同上 |
| **第 4 批** | 移动端打磨 + 组件拆分收尾 + `npm run build` 通过 + 变更说明文档 | 构建通过记录 + 文件清单 |

**做法**：把 `E:\moeshelf-src` 完整复制到 `E:\moeshelf-work`，只改副本；用 `_recon/` 里的 CDP 截图脚本对**同一批视口**做改动前后对照；最后在同一台机器上起 `:3001` 让你直接点。

---

## 4. 需要你确认的三件事

1. **视觉方向**：① 精致克制（Infuse / Steam 式，层级清晰，稳妥）② 影音沉浸（大封面 + 深色玻璃，冲击力强）③ 信息优先（更密更紧凑，一屏看更多）
2. **滚动模型**：① 左栏 sticky + 主区单滚动（推荐）② 维持现在三栏各自滚动 ③ 整个页面一条滚动条
3. **验收节奏**：① 按第 1→4 批逐步验收（推荐）② 一次性全做完再一起看

---

## 附录：`_recon/` 证据清单

| 文件 | 内容 |
|---|---|
| `live-dark-1920.png` / `live-1920.png` | 1920×1080 主界面（暗 / 亮） |
| `live-2560.png` | 2560×1440（宽屏留白证据） |
| `live-1440.png` | 1440×900（中屏拥挤证据） |
| `live-mobile.png` | 390×844 移动端 |
| `modal-detail.png` | 游戏详情弹窗（左栏空白 + 双层滚动） |
| `modal-settings.png` | 设置弹窗（单列表单） |
| `metrics-dark-1920.json` / `metrics-1920.json` | 计算样式与几何量测（三栏宽度、网格列数、字号直方图、文字色分布、滚动容器数） |
| `metrics-modal.json` | 弹窗态量测（选择器需修正，结论以截图为准） |
| `shot.mjs` / `measure.mjs` / `scale-probe.mjs` | 只读侦察工具（CDP 截图 / 量测 / 缩放探针），后续用于改动前后对照 |
