"use strict";
(() => {
  var e = {};
  e.id = 130, e.ids = [130, 147], e.modules = {
    399: e => {
      e.exports = require("next/dist/compiled/next-server/app-page.runtime.prod.js")
    },
    517: e => {
      e.exports = require("next/dist/compiled/next-server/app-route.runtime.prod.js")
    },
    3977: e => {
      e.exports = require("node:fs/promises")
    },
    9411: e => {
      e.exports = require("node:path")
    },
    8229: (e, t, a) => {
      a.r(t), a.d(t, {
        originalPathname: () => g,
        patchFetch: () => x,
        requestAsyncStorage: () => m,
        routeModule: () => p,
        serverHooks: () => h,
        staticGenerationAsyncStorage: () => f
      });
      var r = {};
      a.r(r), a.d(r, {
        POST: () => d,
        dynamic: () => l,
        runtime: () => u
      });
      var n = a(9303),
        s = a(8716),
        o = a(670),
        i = a(7070),
        c = a(2147);
      let l = "force-dynamic",
        u = "nodejs";
      async function d(e) {
        try {
          let t = await e.json(),
            a = t.rootPath?.trim();
          if (!a) return i.NextResponse.json({
            ok: !1,
            error: "缺少根目录路径"
          }, {
            status: 400
          });
          let r = "loose" === t.mode ? "loose" : "strict";
          if (t.path) {
            let p2 = (t.path || "").trim();
            if (p2) {
              try {
                let g2 = await (0, c.identify)(p2, r, a);
                return i.NextResponse.json({
                  ok: !0,
                  game: g2
                })
              } catch {
                return i.NextResponse.json({
                  ok: !0,
                  game: null,
                  error: "路径无效或无法访问"
                })
              }
            }
          }
          let n2 = await (0, c.scanRoot)(a, r);
          return i.NextResponse.json({
            ok: !0,
            ...n2
          })
        } catch (e) {
          return i.NextResponse.json({
            ok: !1,
            error: "扫描失败：" + (e instanceof Error ? e.message : String(e))
          }, {
            status: 500
          })
        }
      }
      let p = new n.AppRouteRouteModule({
          definition: {
            kind: s.x.APP_ROUTE,
            page: "/api/scan/route",
            pathname: "/api/scan",
            filename: "route",
            bundlePath: "app/api/scan/route"
          },
          resolvedPagePath: "C:\\Users\\ADMINI~1\\AppData\\Local\\Temp\\chatbox-sandbox\\9f237e0b-ce8c-47e5-8a20-d38d704468b1\\galgame-library\\src\\app\\api\\scan\\route.ts",
          nextConfigOutput: "standalone",
          userland: r
        }),
        {
          requestAsyncStorage: m,
          staticGenerationAsyncStorage: f,
          serverHooks: h
        } = p,
        g = "/api/scan/route";

      function x() {
        return (0, o.patchFetch)({
          serverHooks: h,
          staticGenerationAsyncStorage: f
        })
      }
    },
    2147: (e, t, a) => {
      a.d(t, {
        B: () => d,
        scanRoot: () => h,
        identify: () => j2
      });
      var r = a(3977),
        n = a.n(r),
        o = a(9411),
        i = a.n(o),
        s = a(644);
      let c = new Set(["setup", "setup1", "setup2", "setup3", "setup_x86", "setup_x64", "unins000", "unins001", "unins002", "uninstall", "uninst", "inst", "install", "installer", "update", "updater", "updata", "patch", "patcher", "config", "configure", "cfg", "autorun", "readme", "eula", "license", "reg", "register", "regist", "vcredist", "vcredist_x86", "vcredist_x64", "vc_redist", "vc_redist.x86", "vc_redist.x64", "dxsetup", "dxwebsetup", "directx", "dotnetfx", "dotnetfx35", "dotnetfx40", "dotnet", "redist", "gxsetup", "crashreporter", "sendto", "msvcp", "msvcr", "d3dx9", "d3dx10", "unarc", "unrar", "7z", "7za", "winrar", "notepad", "cmd", "explorer", "安装", "卸载", "更新", "补丁", "说明", "常见问题", "注册表", "0", "1", "2"]),
        l = new Set(["data", "savedata", "save_data", "save", "bgm", "bgs", "se", "voice", "movie", "movie2", "op", "ed", "system", "sys", "script", "scenario", "scenario2", "cg", "graph", "graphics", "image", "images", "sound", "sounds", "resource", "resources", "font", "fonts", "dll", "common", "patch", "update", "readme", "manual", "doc", "docs", "config", "setting", "settings", "log", "logs", "cache", "temp", "tmp", "debug", "screenshot", "capture", "icon", "icons", "text", "bin", "lib", "libs", "ext", "extensions", "plugin", "plugins", "mod", "mods", "tool", "tools", "backup", "存档", "语音", "音乐", "背景", "立绘", "头像", "补丁", "说明", "文档", "临时", "备份", "字体", "图片", "cg集", "演出", "效果", "脚本", "资源", "数据"]),
        u = ["$recycle.bin", "system volume information", "windows", "program files", "program files (x86)", "node_modules", ".git", ".svn", "msocache", "perflogs", "recovery", "config.msi"];
      async function p(e, t, a, r, o) {
        let s;
        if (!(a > r) && !(t.length >= o)) {
          try {
            s = await n().readdir(e, {
              withFileTypes: !0
            })
          } catch {
            return
          }
          for (let n of s) {
            if (t.length >= o) return;
            n.isFile() ? t.push(n.name) : (n.isDirectory() || n.isSymbolicLink()) && a < r && await p(i().join(e, n.name), t, a + 1, r, o)
          }
        }
      }
      async function d(e) {
        let t = [],
          a = async (e, r, o) => {
            let s;
            if (!(o > 4) && !(t.length >= 60)) {
              try {
                s = await n().readdir(e, {
                  withFileTypes: !0
                })
              } catch {
                return
              }
              for (let n of s) {
                if (t.length >= 60) return;
                let s = i().join(e, n.name);
                if (n.isDirectory() || n.isSymbolicLink()) {
                  let e = n.name.toLowerCase();
                  if (n.name.startsWith(".") || u.includes(e)) continue;
                  await a(s, r ? r + "/" + n.name : n.name, o + 1)
                } else if (n.isFile()) {
                  let e = n.name.toLowerCase();
                  (e.endsWith(".exe") || e.endsWith(".bat") || e.endsWith(".cmd")) && t.push({
                    name: n.name,
                    path: s,
                    rel: r ? r + "/" + n.name : n.name
                  })
                }
              }
            }
          };
        return await a(e, "", 0), t.sort((t, a) => {
          let r = i().dirname(t.path) === e ? 0 : 1,
            n = i().dirname(a.path) === e ? 0 : 1;
          if (r !== n) return r - n;
          let o = t.name.toLowerCase().endsWith(".exe") ? 0 : 1,
            s = a.name.toLowerCase().endsWith(".exe") ? 0 : 1;
          return o !== s ? o - s : t.name.localeCompare(a.name)
        }), t
      }
      async function f(e, t) {
        let a = (await d(e)).filter(e => {
            if (!e.name.toLowerCase().endsWith(".exe")) return !0;
            let t = e.name.slice(0, -4).toLowerCase();
            return !c.has(t) && !/uninstall|uninst|unins\d+|卸载|卸载程序|updater?|updata|setup|installer|config|readme|license|eula|regedit|regedt|register|registry|vcredist|redist|dxsetup|dxwebsetup|directx|dotnet/.test(t)
          }).map(e => ({
            name: e.name,
            path: e.path
          })),
          r = t.toLowerCase().replace(/[^\w\u4e00-\u9fa5]/g, ""),
          h2 = n => /(^|[^a-z0-9])(chs|sc|cn|crack|汉化|中文|chinese|cht)([^a-z0-9]|$)/i.test(n) || /汉化版|中文版|破解版|整合版|hardcoded/.test(n);
        return a.sort((e, t) => {
          let a = e.name.toLowerCase(),
            n = t.name.toLowerCase(),
            ga = h2(a) ? 0 : 1,
            gb = h2(n) ? 0 : 1;
          if (ga !== gb) return ga - gb;
          let a2 = a.replace(/[^\w\u4e00-\u9fa5]/g, ""),
            b2 = n.replace(/[^\w\u4e00-\u9fa5]/g, "");
          return (r ? a2.includes(r) ? 0 : m(a2, r) : 0) - (r ? b2.includes(r) ? 0 : m(b2, r) : 0)
        }), a.slice(0, 10)
      }

      function m(e, t) {
        let a = e.length,
          r = t.length;
        if (!a) return r;
        if (!r) return a;
        let n = Array.from({
          length: r + 1
        }, (e, t) => t);
        for (let o = 1; o <= a; o++) {
          let a = [o];
          for (let i = 1; i <= r; i++) a[i] = Math.min(n[i] + 1, a[i - 1] + 1, n[i - 1] + (e[o - 1] === t[i - 1] ? 0 : 1));
          n = a
        }
        return n[r]
      }
      async function h(e, t) {
        let r = [],
          o = 0,
          rm0 = [];
        async function k(d, m) {
          if (m > 4) return;
          let a;
          try {
            a = await n().readdir(d, {
              withFileTypes: !0
            })
          } catch {
            return
          }
          for (let n of a) {
            if (!n.isDirectory() && !n.isSymbolicLink()) continue;
            let a = n.name,
              x = a.toLowerCase();
            if (a.startsWith(".") || u.includes(x) || x.includes("galtools") || x.includes("解压") || x.includes("解包") || x.includes("提取工具") || x.includes("汉化补丁") || x.includes("备份") || x === "tools" || x === "tool" || x === "backup" || x === "补丁" || x === "patch" || x === "crack" || x === "破解" || x === "directx" || x === "运行库" || x === "vcredist") {
              o++;
              continue
            }
            let p2 = i().join(d, a),
              h2 = [];
            await p(p2, h2, 0, 0, 5e3);
            let {
              isGame: g,
              score: w,
              matchedTypes: y
            } = function(e, t, a) {
              let r = new Map,
                n = new Set,
                o = 0;
              let b = [];
              for (let e of t) {
                let t = e.lastIndexOf(".");
                if (t <= 0) continue;
                let a = e.slice(t).toLowerCase(),
                  w = e.slice(0, t).toLowerCase(),
                  i = function(e, t) {
                    switch (e) {
                      case ".xp3":
                        return 6;
                      case ".nsa":
                      case ".noa":
                      case ".nmc":
                      case ".cvd":
                        return 5;
                      case ".exe":
                        return c.has(t) ? 1 : 4;
                      case ".iso":
                      case ".img":
                      case ".mdf":
                      case ".mds":
                        return 4;
                      case ".arc":
                      case ".pak":
                      case ".ks":
                      case ".tjs":
                      case ".pkd":
                      case ".pfs":
                      case ".s3r":
                      case ".ypf":
                      case ".mpk":
                      case ".pck":
                      case ".pac":
                      case ".ald":
                      case ".g00":
                      case ".sdc":
                      case ".xfl":
                      case ".ns2":
                        return 2;
                      case ".dat":
                      case ".bin":
                      case ".dll":
                      case ".txt":
                        return 1;
                      case ".rpa":
                      case ".rpi":
                        return 6;
                      case ".rvdata2":
                      case ".rvdata":
                      case ".rxdata":
                      case ".ldb":
                      case ".lmu":
                      case ".wolfsave":
                      case ".int":
                      case ".majiro":
                      case ".assets":
                      case ".unity3d":
                        return 4;
                      case ".lcf":
                      case ".wolfdata":
                      case ".stx":
                      case ".maj":
                      case ".rpy":
                      case ".rpyc":
                      case ".resS":
                      case ".bundle":
                      case ".nsc":
                      case ".obb":
                        return 3;
                      case ".dat2":
                        return 2;
                      default:
                        return 0
                    }
                  }
(a, w);
                ".exe" === a && b.push(w), i && (n.add(a), o++, i > (r.get(a) ?? 0) && r.set(a, i))
              }
              let i = 0;
              for (let e of r.values()) i += e;
              1 === o && r.has(".exe") && b.length && b.every(x => c.has(x)) && (i = Math.min(i, 2));
              return {
                isGame: b.some(x => !c.has(x)) || i >= ("loose" === a ? 2 : 3),
                score: i,
                matchedTypes: [...n].sort()
              }
            }
(a, h2, t);
            if (g) {
              let x2 = await f(p2, a);
              r.push({
                folderName: a,
                folderPath: p2,
                pathHash: (0, s.pb)(p2),
                fileCount: h2.length,
                exeCandidates: x2,
                matchScore: w,
                matchedTypes: y
              })
            } else {
              o++;
              await k(p2, m + 1)
            }
          }
        }
        return await k(e, 0), await (async () => {
          try {
            let z = JSON.parse(await n().readFile(i().join(process.cwd(), "data", "settings.json"), "utf-8")),
              q = z.ignorePaths || [];
            if (q.length) r = r.filter(g => !q.includes(g.folderPath))
          } catch {}
        })(), r.sort((e, t) => e.folderName.localeCompare(t.folderName, "zh-Hans-CN")), await (async () => {
          let rm = [];
          try {
            let z = JSON.parse(await n().readFile(i().join(process.cwd(), "data", "library.json"), "utf-8")),
              gs = Array.isArray(z.games) ? z.games : [];
            for (let g of gs) {
              if (!g || !g.folderPath) continue;
              try {
                let st = await n().stat(g.folderPath);
                if (!st.isDirectory()) rm.push(g.pathHash)
              } catch {
                rm.push(g.pathHash)
              }
            }
          } catch {}
          rm0 = rm
        })(), {
          games: r,
          skipped: o,
          removedPaths: rm0
        }
      }
      async function j2(e, t, a) {
        let h2 = [];
        await p(e, h2, 0, 0, 5e3);
        let nm = i().basename(e),
          r2 = new Map,
          n2 = new Set,
          o2 = 0,
          b2 = [];
        for (let f2 of h2) {
          let t2 = f2.lastIndexOf(".");
          if (t2 <= 0) continue;
          let x2 = f2.slice(t2).toLowerCase(),
            w2 = f2.slice(0, t2).toLowerCase(),
            v2 = function(e, t) {
              switch (e) {
                case ".xp3":
                  return 6;
                case ".nsa":
                case ".noa":
                case ".nmc":
                case ".cvd":
                  return 5;
                case ".exe":
                  return c.has(t) ? 1 : 4;
                case ".iso":
                case ".img":
                case ".mdf":
                case ".mds":
                  return 4;
                case ".arc":
                case ".pak":
                case ".ks":
                case ".tjs":
                case ".pkd":
                case ".pfs":
                case ".s3r":
                case ".ypf":
                case ".mpk":
                case ".pck":
                case ".pac":
                case ".ald":
                case ".g00":
                case ".sdc":
                case ".xfl":
                case ".ns2":
                  return 2;
                case ".dat":
                case ".bin":
                case ".dll":
                case ".txt":
                  return 1;
                case ".rpa":
                case ".rpi":
                  return 6;
                case ".rvdata2":
                case ".rvdata":
                case ".rxdata":
                case ".ldb":
                case ".lmu":
                case ".wolfsave":
                case ".int":
                case ".majiro":
                case ".assets":
                case ".unity3d":
                  return 4;
                case ".lcf":
                case ".wolfdata":
                case ".stx":
                case ".maj":
                case ".rpy":
                case ".rpyc":
                case ".resS":
                case ".bundle":
                case ".nsc":
                case ".obb":
                  return 3;
                case ".dat2":
                  return 2;
                default:
                  return 0
              }
            }
(x2, w2);
          ".exe" === x2 && b2.push(w2), v2 && (n2.add(x2), o2++, v2 > (r2.get(x2) ?? 0) && r2.set(x2, v2))
        }
        let i2 = 0;
        for (let e of r2.values()) i2 += e;
        1 === o2 && r2.has(".exe") && b2.length && b2.every(x => c.has(x)) && (i2 = Math.min(i2, 2));
        if (!(b2.some(x => !c.has(x)) || i2 >= ("loose" === t ? 2 : 3))) return null;
        return {
          folderName: nm,
          folderPath: e,
          pathHash: (0, s.pb)(e),
          fileCount: h2.length,
          exeCandidates: await f(e, nm),
          matchScore: i2,
          matchedTypes: [...n2].sort(),
          rootPath: a || ""
        }
      }
    },
    644: (e, t, a) => {
      function r(e) {
        let t = 5381;
        for (let a = 0; a < e.length; a++) t = (t << 5) + t + e.charCodeAt(a) >>> 0;
        return t.toString(16).padStart(8, "0")
      }

      function n(e, t) {
        return `${
e}::${
t}
`
      }

      function s(e) {
        return new Promise(t => setTimeout(t, e))
      }

      function o(e, t) {
        return e && t ? (e = e.trim().toLowerCase()) === (t = t.trim().toLowerCase()) ? 1 : e.includes(t) || t.includes(e) ? .85 : 1 - function(e, t) {
          let a = e.length,
            r = t.length;
          if (!a) return r;
          if (!r) return a;
          let n = Array.from({
            length: r + 1
          }, (e, t) => t);
          for (let s = 1; s <= a; s++) {
            let a = [s];
            for (let o = 1; o <= r; o++) a[o] = Math.min(n[o] + 1, a[o - 1] + 1, n[o - 1] + (e[s - 1] === t[o - 1] ? 0 : 1));
            n = a
          }
          return n[r]
        }
(e, t) / Math.max(e.length, t.length) : 0
      }

      function i(e, t, a, r = .4) {
        let n = null,
          s = 0;
        for (let r of t) {
          let t = o(e, a(r));
          t > s && (s = t, n = r)
        }
        return s >= r ? n : null
      }
      a.d(t, {
        Li: () => i,
        _v: () => s,
        hY: () => o,
        kw: () => n,
        pb: () => r
      })
    }
  };
  var t = require("../../../webpack-runtime.js");
  t.C(e);
  var a = e => t(t.s = e),
    r = t.X(0, [948, 972], () => a(8229));
  module.exports = r
})();