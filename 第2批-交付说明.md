# 第 2 批交付说明 · 左侧导航 + 右栏 + 空态骨架

> 工作副本：`E:\moeshelf-work`（原始目录 `E:\moeshelf-src` 始终只读，`git status` 干净）
> 预览地址：**http://localhost:3001**（后台 dev 服务运行中，真实游戏库副本 187 个游戏）
> 前置批次：第 1 批（设计令牌 + rem 排版 + 滚动模型 + 卡片重设计）已完成验收

---

## 1. 这一批改了什么

### 左栏 → 真正的导航栏（原来只是一个厂商列表）

| 项目 | 改动 |
|---|---|
| **概览数字** | 顶部新增「收藏 / 通关 / 时长」三栏小统计，一进页面就知道库的规模 |
| **分区导航** | 「全部游戏」「我的收藏」成为独立条目（带图标、可高亮、显示数量）；「我的收藏」点击即切换右上工具条的「仅收藏」 |
| **厂商搜索** | 新增筛选输入框，189 个厂商里直接搜（原来只能滚） |
| **渐进展开** | 默认只列 18 个常用厂商 + 「还有 N 个厂商 · 展开」，避免 6000px 长的列表把侧栏撑爆；另有「全部厂商 / 仅当前」两个快捷筛选 |
| **折叠成图标条** | 底部「收起侧栏」把 232px 折成 52px 图标条，宽度让给卡片；状态记忆在 `localStorage` |
| **加载态** | 厂商行右侧的「获取中 22/23」文字改成行内 spinner + 工具条上的「补齐厂商 22/23」轻量进度 |
| **语义清晰** | 原来标题行「厂商 / 获取中 / 名称 ↑」三组元素挤在一起、箭头看不出在排什么 → 现在分区标题带图标，排序按钮明确标注「升序 / 降序」并给出 tooltip |

### 右栏 → 去掉冗余、合并同类项

| 项目 | 改动 |
|---|---|
| **统计摘要** | 「总时长」大字保留，下面改成 **游戏 / 通关 / 收藏** 三栏（补上收藏数） |
| **合并两块列表** | 原来「最近启动」和「已通关」各占一块、各自滚动；现在合并为一个面板，顶部用**分段控件切换**，只保留一条滚动区；列表可视高度 240/300 → 380px |
| **视觉统一** | 全部改用 `panel` 令牌、13px 行高、`section-label` 小标题 |

### 空态与骨架屏

| 项目 | 改动 |
|---|---|
| **骨架屏** | 首次进入（`/api/*` 还没回来）不再闪一下整屏空白：按当前密度铺满一屏卡片骨架 + 左栏/右栏骨架，带 shimmer 动画；`prefers-reduced-motion` 下自动关闭动画 |
| **空态重做** | 从「大图标 + 一段话 + 大按钮」改成**产品化空态**：标题「把你的 Galgame 收藏摆上书架」+ 三步走说明卡（选择目录 / 自动识别 / 抓取信息）+ 主按钮 + 隐私提示；全部换成语义色令牌，亮色主题不再白底白字 |
| **顶栏图标** | 🌙/☀️ emoji 换成线条 SVG（与其它图标同一套语言），新增 `sun`/`moon`/`panelLeft`/`layers`/`checkCircle`/`chart` 图标 |

---

## 2. 复验结果（脚本 `_recon/verify-batch2.mjs`、`verify2b.mjs`、`verify2c.mjs`）

| 验证项 | 结果 |
|---|---|
| 左栏结构 | 3 栏统计 ✅ · 有厂商搜索框 ✅ · 有收起按钮 ✅ |
| 厂商搜索「key」 | 列表从 18+ 行收敛到 **1 行（Key 5/28）** ✅ |
| 「全部厂商」展开 | 侧栏按钮数 **37 → 94** ✅ |
| 厂商跳转 | 滚动 2400px 后点 YuzuSoft → `scrollY = 0`、标题 `YuzuSoft`、60 张卡 ✅ |
| 侧栏折叠 | 网格列宽 `232px 1328px 264px` → **`52px 1508px 264px`**（卡片区 +180px）✅ |
| 右栏切换 | 点「已通关 32」→ 列出通关游戏并带序号 ✅ |
| 收藏联动 | 点卡片星标 → 左栏「我的收藏 **0 → 1**」、右栏「收藏 **0 → 1**」✅ |
| 骨架屏 | 拦截 `/api/*` 9 秒后截图：88 个骨架元素、0 张真实卡片，布局与真实网格一致 ✅ |
| 生产构建 | `npm run build` → **Compiled successfully，exit 0** ✅ |
| 类型检查 | `npx tsc --noEmit` → 0 错误 ✅ |

---

## 3. 验收材料（`_recon/`）

| 文件 | 内容 |
|---|---|
| `after2-1920-light.png` / `after2-1920-dark.png` | 第 2 批后的主界面（已含新左栏、新右栏） |
| `verify-skeleton-1920.png` | 首次加载骨架屏（延迟 API 后抓取） |
| `verify2-rail-search.png` | 厂商搜索命中效果 |
| `verify2-rail-collapsed.png` | 侧栏折叠成图标条 |
| `verify2-aside-completed.png` | 右栏切到「已通关」 |
| `verify2-company.png` | 厂商视图（含分页修复） |
| `第1批-交付说明.md` | 第 1 批的完整交付说明（量测对比、改动清单） |

---

## 4. 下一步（第 3 批）

1. **游戏详情弹窗**：左栏封面下方 400px 空白填掉、评分/厂商/发售日上移、简介与角色改成标签页（不再套两层滚动）、主操作（启动/打开文件夹）提到弹窗顶部；
2. **设置弹窗**：单列表单改「左侧分组导航 + 右侧内容」，说明文字折叠为 `?` 气泡，危险操作单独分组，按钮统一成主/次两档；
3. 「界面缩放」并入外观分组，并说明它现在真的会缩放文字（第 1 批已把字号改成 rem）。

第 4 批：移动端专门打磨 + `page.tsx`（约 4,900 行）组件拆分 + 构建收尾。
