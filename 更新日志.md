# 更新日志

## 1.6.0（2026-09-08）

### 新增功能

- **新增 Moyu（www.moyu.moe，鲲 Galgame 补丁）数据源**：接入站方公开 JSON API（无鉴权、无 HTML 解析），作为第 5 个数据源，主要解决「游戏简介不实用」的问题
  - **中文简介自动补全优先改用 Moyu**：详情面板打开时优先拉取 Moyu 条目的多语言简介（zh-cn/zh-tw 优先，自动清洗 markdown 并截取 STORY 剧情段落），CnGal/YMgal/Bangumi 依次兜底；找不到中文时不把日文简介里的汉字误当中文
  - **全源搜索与手动修正支持 Moyu**：「修正条目」下拉新增 Moyu 选项，「全部数据源」搜索也会列出 Moyu 结果，可手动按 Moyu 条目匹配
  - **自动刮削末位兜底**：顺序改为 vndb → bangumi → ymgal → cngal → moyu，前四源都失败时尝试 Moyu，扩大老作品/同人作品命中率
  - 带回中文名/原名、封面（走站点图片 CDN）、发售日、厂商、VNDB 评分、VNDB ID 与条目链接等元数据
- 版本号更新至 1.6.0，并基于当前最新源码重新构建发布目录

### 修复

- 修复 Moyu 源对 R18 条目"搜得到却取不到"：站方接口默认 SFW 过滤，NSFW 条目的详情请求会被 404；现在所有 Moyu 详情请求统一带 `content_limit=all`（只取文字简介/元数据），拔作岛等 R18 作品可正常匹配与取简介

---

## 1.5.2（2026-08-15）

### 修复

- 修复：四个数据源都刮削失败时被误判为成功、清空已有信息，且不会自动重试；现在会明确标记失败，保留旧元数据
- 修复：重新扫描或移除游戏后，全库「已通关」标记与通关时间可能被旧字段请求覆盖清空
- 修复：数据目录二次迁移会复制旧数据（现在从当前生效的数据目录复制）；扫描忽略列表与失效目录统计现在尊重自定义数据目录
- 修复：启动 `.bat` / `.cmd` 脚本时 `spawn EINVAL`，现在通过 `cmd.exe` 正确启动；启动不存在的文件会在响应中直接报错
- 修复：资料库备份导入后丢失 `completedAt`（通关时间）
- 修复：修改游戏路径后丢失自定义 exe/封面/标题/厂商映射与游玩时长；现在服务端自动迁移关联数据，详情弹窗保持打开，刮削缓存键一致
- 修复：重新扫描后不再保留已删除/已移动的游戏目录，并刷新已有条目的 exe 候选、文件数、匹配分数
- 修复：游戏详情弹窗条件渲染早于 Hooks 返回的隐患
- 修复：设置里切换全局 NSFW 模糊后卡片不能即时刷新
- 修复：清空刮削缓存时未清理 `cache/images` 封面图片缓存
- 修复：VNDB 直接刮削结果缺少封面分级，导致本地 R18 模糊依赖会社列表请求
- 修复：角色获取没有总超时；超时不再写入空结果缓存，下次打开可重试
- 修复：手动获取角色时 VNDB/YMgal 被误显示为 CnGal
- 修复：代理回退提示由全局计数改为按请求隔离，避免并发请求误报
- 修复：Web App Manifest 返回错误的 Content-Type
- 修复：源码目录下 `启动.bat` 无法直接启动（现在检测打包版 `server.js`，源码目录构建后自动改用 `next start`）
- 修复：收藏游戏在全部游戏视图中不置顶的问题
- 调整：卡片右上角已通关标识改用项目内部 Icon 组件（对勾图标），深色玻璃圆底 + 香槟金光晕，与其他图标对齐统一
- 修正：1.5.0 更新日志中“自定义标签”尚未实现，已从说明中移除；`*.tsbuildinfo` 加入 .gitignore

### 版本

- 版本号更新至 1.5.2，并基于当前最新源码重新构建发布目录

---

## 1.5.1（2026-08-15）

### 新增功能

- **已通关体系**：游戏详情中可标记「已通关」，通关时间自动记录；右侧栏新增「已通关」板块（数量统计 + 封面缩略图列表，按通关时间倒序排列，最新通关置顶，点击打开游戏详情）；已通关卡片右上角显示 Fin 丝带角标（金色渐变缎带 + 中央对勾徽章，经典斜向裁切效果，移动端同样生效）
- **单游戏 NSFW 模糊开关**：卡片封面左下角眼睛按钮可单独开启/关闭该游戏的封面模糊（按游戏持久化），详情弹窗点击 NSFW 标签亦可切换

### 优化

- 左右侧栏固定定位：滚动页面时侧栏纹丝不动、紧贴顶栏下沿不被遮挡；会社列表与已通关列表超高时在列表内部滚动（悬停滚轮只滚动列表，不带动页面），列表高度有界不无限拉长
- 右侧「最近启动」条目点击改为打开游戏详情，另设独立启动按钮保留快捷启动
- 代理不可用提示只提示一次：后台自动补全/重试失败保持静默，仅手动操作时给出明确提示，避免反复弹窗

### 修复

- NSFW 封面模糊导致本地游戏封面变黑白（移除灰度滤镜，仅保留高斯模糊与 -webkit-filter 移动端兼容）
- 模糊封面边缘出现彩色色圈（封面存在时不再叠加渐变占位层，边缘与深色背景自然融合）
- 代理软件未开启时刮削/搜索/封面全部无法加载（代理连接失败自动回退直连重试，失败原因完整透出）

### 变更

- 移除 Bangumi 评分模块：卡片右上角评分徽章、设置面板「显示选项」开关、详情页 BGM 评分展示一并下线
- CSV 导出「评分」列改用 VNDB 评分（无 VNDB 评分时留空）
- 移除左侧栏「年份」分组与移动端年份筛选（「发售日」排序保留）
- 版本号更新至 1.5.1

---

## 1.5.0（源码重建版）

- 从 1.4.2 编译产物重建完整源码工程（功能与数据格式保持一致）
- 修复：未下载游戏卡片「未下载」按钮图标几乎不可见（禁用状态 35% 透明度 + 线条图标被错误填充），现在图标清晰可见
- 新增：数据自动备份（data/backup，保留最近 5 份）
- 新增：资料库一键导出/导入（设置面板）
- 优化：目录选择器不再记忆上次路径，每次从头选择
- 新增：资料库导出为 CSV（可用 Excel 打开）

---

## 1.4.0（2026-08-06）

### 新增功能

- **会社视角展示未下载游戏**（VNDB 数据源）
  - 左侧会社名右侧数字升级为「本地/全部」格式（如 14/14），本地缺失时显示 13/14
  - 选中会社后，列表末尾展示该会社尚未下载的游戏，仅出现在会社筛选视图，全部游戏栏不受影响
  - 未下载游戏卡片：封面灰色滤镜 + 「未下载」标记，底部按钮显示「未下载」，点开详情完整展示评分/发售日/简介/角色
  - 未下载游戏支持修改封面/标题/厂商（本地持久化），也可从列表移除
  - 会社数据由 VNDB 刮削并永久缓存，首次加载后秒开；空结果自动重拉，杜绝缓存污染
- **中文游戏名**：本地游戏与未下载游戏统一优先显示官方中文/民间中文名（VNDB 别名 + ymgal/cngal/bangumi 搜索，带相似度校验），找不到时保留原名
- **收藏功能**：卡片星标收藏（金色高亮），收藏游戏置顶，工具栏可切换「仅显示收藏」
- **R18 封面高斯模糊 + NSFW 标记**：按 VNDB 封面分级（image.sexual）判断封面本身是否 R18（如借恋正篇不模糊、FD 封面模糊），封面居中显示无衬线 NSFW 大字，设置中可一键关闭
- **启动 exe 智能选择**：优先识别汉化/破解版主程序（chs/cn/crack/汉化等字样），自动排除卸载/安装/更新/环境依赖等无关程序（uninstall/卸载/setup/vcredist 等）

### 优化

- 未下载数据拉取提速：首次加载由 17 秒降至约 2.3 秒（中文名搜索改为三源并行 + 后台补全）
- 会社获取进度可视化：「厂商」栏实时显示「获取中 x/y」
- 会社名匹配增强：优先使用本地游戏自带的 VNDB 开发者原名搜索，搜不到时用 vndbId 反查制作商，避免日文/中文会社名无法识别
- Bangumi 评分获取超时放宽至 4 秒，评分命中率显著提升；缺失评分可重新获取信息补齐
- 代理设置：输入框留空回车自动填入默认代理 http://127.0.0.1:7890
- 未下载空列表/失败状态显示明确提示与「重新获取」按钮
- 移除设置中的「游戏识别严格度」板块

### 修复

- 修复「选择目录后 Application error」：J() 主组件内图标引用变量被遮蔽导致的运行时崩溃
- 修复星标不显示：黄色样式类未编译进 CSS，改用内联样式 + 内联 SVG，确保任何环境可见
- 修复移动端灰色滤镜失效：补全 `-webkit-filter` 前缀
- 修复未下载封面半透明透出占位符问题
- 修复会社空列表被永久缓存导致始终无法加载（自动重拉修复）
- 更新无需强制刷新：页面 JS 更换文件名 hash，浏览器自动加载新版本
