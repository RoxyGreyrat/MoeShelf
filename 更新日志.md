# 更新日志

本项目所有值得记录的变更都会汇总在本文件中，按版本倒序排列。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)规范。

版本号规则：`主版本.次版本.修订号`
- **新增**：新能力或新功能；
- **优化**：体验、性能或稳定性改进（不改变既有行为）；
- **修复**：Bug 修复；
- **变更 / 移除**：行为调整或下线的能力。

---

## 1.6.1 - 2026-09-08

### 新增

- **SQLite 数据持久化**：`library / playtime / cache` 三个 JSON 的内部存储迁移到 `data/moeshelf.db`（better-sqlite3，WAL + busy_timeout）。
  - 首次启动自动检测旧 JSON 并无损迁移（迁移前把三份 JSON 备份到 `data/migration-backup/`，原文件保留）；迁移失败自动回滚，不破坏旧数据。
  - 数据目录迁移使用 SQLite `backup()` 生成一致性副本，WAL 场景安全；自动备份为 `data/backup/moeshelf.YYYYMMDD-HHmmss.db`（保留最近 5 份）。
  - `settings.json / location.json / cache/images/` 继续使用文件存储，保持原语义。
- **新增 db 分层**：`src/lib/db/`（schema / games / playtime / scrape-cache / migration），`core.ts` 只替换内部 I/O——**API 与前端数据结构不变**；JSON 导入 / 导出格式与旧版兼容（SQLite 为内部存储、JSON 为用户交换格式）。
- **UI 亮 / 暗主题切换**：顶栏 🌙/☀ 开关，默认跟随系统并记忆选择；大窗口内容加宽（上限 1880px），桌面窗口默认 1560×940、最小 1320×760，避免右栏在窗口过小时消失。
- 依赖新增 `better-sqlite3`（原生模块，首次构建需联网安装）。

### 修复与优化

- **亮色主题改版（不再整体反色）**：封面/截图等图片不做任何滤镜处理（保持原图）；底色、卡片、边框直接切换浅色；文字按原透明度分级映射为柔和深灰（标题/正文深色、次要/标签清晰可读），选中态与强调色（靛蓝、绿色、琥珀"已通关"等）单独加深；彩色/渐变按钮与黑底浮层上的文字保持白色。
- 修复亮暗切换：首次点击即可生效；切换瞬间全站禁用过渡，避免大量元素同时补间造成卡顿。
- 游玩时长展示优化：卡片与详情中的时钟字符替换为内联 SVG 时钟；已玩时长文字改为发光翠绿、更显眼。
- 卡片交互细节：移除封面左上角"未下载"徽章；底部"未下载"按钮改为琥珀色高可读样式（暗色/亮色均清晰）。
- **SQLite 稳定性**：进程退出/中断（exit/SIGINT/SIGTERM）时优雅关闭数据库句柄，修复 Node 24 退出清理阶段的原生断言崩溃；better-sqlite3 升级至 v12（含 Node 24 预编译），17 项验证在 Node24 + v12 下全绿。
- **详情面板排版**：弹窗铺满窗口高度，左侧封面区自适应填充（模糊底铺满、封面按原始比例完整显示），评分 / 厂商 / 发售日以圆角标签贴在封面下方底部；右侧简介框高度随窗口动态伸缩，长简介在框内滚动，「主要角色」固定贴底、与简介框保持 12px 间距，弹窗底部不再留空白。
- **设置弹窗**：标题由「管理（设置）」改为「设置」，移除左侧分栏目录，7 项设置默认全部展开并按两列排布。
- **滚动条自动隐藏**：静止时不显示，滚动时淡入、停止约 0.9 秒后淡出（原生滚动条伪元素不支持过渡，改为隐藏原生条 + 自定义指示条）。
- **修复会社归类**：厂商别名规则 `/akabeisoft/i` 过于宽泛，导致 AKABEiSOFT3（如「生命的备件」）被并入 AKABEiSOFT2；改为精确匹配 `akabeisoft2` / `akabeisoft3`（兼容日文名「あかべぇそふとつぅ / あかべぇそふとすりぃ」），两个品牌各归各家。

### 其他

- 验证脚本 `scripts/verify-sqlite.mjs`：空目录启动、旧 JSON 首迁、数量一致性、增删改、游玩累计、缓存过期、路径修改、数据目录迁移、JSON 导入导出、自动备份、重启保持等 17 项断言——本地与 GitHub Actions 均通过；`npm run build` 通过。
- 版本号更新至 1.6.1。

---

## 1.6.0 - 2026-09-08

### 新增

- **接入第 5 个数据源 Moyu**（[moyu.moe](https://www.moyu.moe) / 鲲 Galgame 补丁，开源项目 [kun-galgame-patch](https://github.com/KunMoe/kun-galgame-patch)）：基于站方公开 JSON API（无需鉴权、无 HTML 解析），用于提升中文简介质量。
- **中文简介自动补全优先改用 Moyu**：详情面板优先拉取条目的多语言简介（`zh-cn` / `zh-tw` 优先），自动清洗 Markdown 并截取 STORY 剧情段落作为正文；仅当存在真正的中文简介时才返回，避免把日文简介中的汉字误判为中文；CnGal / YMgal / Bangumi 依次兜底。
- **Moyu 纳入全源搜索与手动修正**：「修正条目」数据源下拉新增 Moyu，「全部数据源」搜索同步展示 Moyu 结果，可手动按 Moyu 条目应用元数据。
- **自动刮削增加 Moyu 末位兜底**：刮削顺序调整为 `vndb → bangumi → ymgal → cngal → moyu`，前四源均未命中时尝试 Moyu，提升老作品与同人作品的命中率。
- **带回 Moyu 元数据**：中文名/原名、封面（站点图片 CDN）、发售日、厂商、VNDB 评分与 ID、条目来源链接。

### 修复

- **Moyu R18 条目「搜得到却取不到」**：站方详情接口默认启用 SFW 过滤，NSFW 条目（如拔作岛等）不带参数请求会返回 404；现所有 Moyu 详情请求统一携带 `content_limit=all`（仅用于文字简介与元数据，不涉及任何补丁资源下载），R18 作品可正常匹配与获取简介。

### 其他

- 版本号更新至 1.6.0，并基于最新源码重新构建发布目录。

---

## 1.5.2 - 2026-08-15

### 修复

- 四源全部刮削失败时被误判为成功并清空已有信息，且不会自动重试：现改为明确标记失败并保留旧元数据。
- 重新扫描或移除游戏后，「已通关」标记与通关时间可能被旧字段请求覆盖清空。
- 数据目录二次迁移会复制旧数据：现改为从当前生效的数据目录复制；扫描忽略列表与失效目录统计尊重自定义数据目录。
- 启动 `.bat` / `.cmd` 脚本时触发 `spawn EINVAL`：现改经 `cmd.exe` 启动；启动不存在的文件会在响应中直接报错。
- 资料库备份导入后丢失 `completedAt`（通关时间）。
- 修改游戏路径后丢失自定义 exe / 封面 / 标题 / 厂商映射与游玩时长：现由服务端自动迁移关联数据，详情弹窗保持打开，刮削缓存键保持一致。
- 重新扫描后仍保留已删除/已移动的游戏目录：现会移除失效条目，并刷新已有条目的 exe 候选、文件数与匹配分数。
- 游戏详情弹窗条件渲染早于 Hooks 返回导致的潜在崩溃。
- 设置中切换全局 NSFW 模糊后，卡片封面不能即时刷新。
- 清空刮削缓存时未清理 `cache/images` 封面图片缓存。
- VNDB 直接刮削结果缺少封面分级，导致本地 R18 模糊需要依赖会社列表请求。
- 角色获取缺少总超时；超时后不再写入空结果缓存，下次打开可重试。
- 手动获取角色时，VNDB / YMgal 结果被误显示为 CnGal。
- 代理回退提示由全局计数改为按请求隔离，避免并发请求互相误报。
- Web App Manifest 返回错误的 Content-Type。
- 源码目录下 `启动.bat` 无法直接启动：现自动检测打包版 `server.js`，源码目录构建完成后改用 `next start`。
- 收藏游戏在「全部游戏」视图中不置顶。

### 变更

- 「已通关」卡片右上角标识改用项目内部 Icon 组件（对勾图标），深色玻璃圆底 + 香槟金光晕，与其他图标风格统一。
- 移除 1.5.0 更新日志中尚未实现的「自定义标签」说明；`*.tsbuildinfo` 加入 `.gitignore`。

---

## 1.5.1 - 2026-08-15

### 新增

- **已通关体系**：详情中可标记「已通关」并自动记录通关时间；右侧栏新增「已通关」板块（数量统计 + 封面缩略图列表，按通关时间倒序、最新置顶）；已通关卡片右上角显示 Fin 丝带角标（金色渐变 + 对勾徽章，移动端同样生效）。
- **单游戏 NSFW 模糊开关**：卡片封面左下角眼睛按钮可单独开关该游戏的封面模糊（按游戏持久化）；详情弹窗点击 NSFW 标签亦可切换。

### 优化

- 左右侧栏改为固定定位：页面滚动时侧栏不位移、贴顶不被遮挡；会社列表与已通关列表超高时在内部滚动，列表高度有界。
- 右侧「最近启动」条目点击改为打开游戏详情，另设独立启动按钮保留快捷启动。
- 代理不可用提示只提示一次：后台自动补全/重试失败保持静默，仅在手动操作时提示，避免反复弹窗。

### 修复

- NSFW 封面模糊导致本地游戏封面变黑白（移除灰度滤镜，仅保留高斯模糊与 `-webkit-filter` 移动端兼容）。
- 模糊封面边缘出现彩色色圈（封面存在时不再叠加渐变占位层）。
- 代理软件未开启时刮削/搜索/封面全部无法加载（代理连接失败自动回退直连重试，失败原因完整透出）。

### 变更

- 移除 Bangumi 评分模块：卡片右上角评分徽章、设置面板「显示选项」开关、详情页 BGM 评分展示一并下线。
- CSV 导出「评分」列改用 VNDB 评分（无 VNDB 评分时留空）。
- 移除左侧栏「年份」分组与移动端年份筛选（保留「发售日」排序）。
- 版本号更新至 1.5.1。

---

## 1.5.0 - 源码重建版

### 新增

- 从 1.4.2 编译产物重建完整源码工程（功能与数据格式保持一致）。
- 数据自动备份（`data/backup`，保留最近 5 份）。
- 资料库一键导出 / 导入（设置面板）。
- 资料库导出为 CSV（可用 Excel 打开）。

### 优化

- 目录选择器不再记忆上次路径，每次从头选择。

### 修复

- 未下载游戏卡片「未下载」按钮图标几乎不可见（禁用态 35% 透明度 + 线条图标被错误填充），现图标清晰可见。

---

## 1.4.0 - 2026-08-06

### 新增

- **会社视角展示未下载游戏**（VNDB 数据源）：
  - 左侧会社名右侧计数升级为「本地/全部」格式（如 `14/14`），本地缺失时显示 `13/14`；
  - 选中会社后，列表末尾展示该会社尚未下载的游戏（仅出现在会社筛选视图）；
  - 未下载游戏卡片：封面灰色滤镜 + 「未下载」标记，详情完整展示评分/发售日/简介/角色；
  - 未下载游戏支持修改封面/标题/厂商（本地持久化），也可从列表移除；
  - 会社数据由 VNDB 刮削并永久缓存，首次加载后秒开；空结果自动重拉，杜绝缓存污染。
- **中文游戏名**：本地游戏与未下载游戏统一优先展示官方/民间中文名（VNDB 别名 + ymgal / cngal / bangumi 搜索，带相似度校验），找不到时保留原名。
- **收藏功能**：卡片星标收藏（金色高亮），收藏游戏置顶，工具栏可切换「仅显示收藏」。
- **R18 封面高斯模糊 + NSFW 标记**：按 VNDB 封面分级（`image.sexual`）判断封面是否为 R18，封面居中显示无衬线 NSFW 大字，设置中可一键关闭。
- **启动 exe 智能选择**：优先识别汉化/破解版主程序（`chs` / `cn` / `crack` / 「汉化」等），自动排除卸载/安装/更新/环境依赖等无关程序。

### 优化

- 未下载数据拉取提速：首次加载由约 17 秒降至约 2.3 秒（中文名搜索改为三源并行 + 后台补全）。
- 会社获取进度可视化：「厂商」栏实时显示「获取中 x/y」。
- 会社名匹配增强：优先用本地游戏自带的 VNDB 开发者原名搜索，搜不到时用 vndbId 反查制作商，避免日文/中文会社名无法识别。
- Bangumi 评分获取超时放宽至 4 秒，评分命中率显著提升；缺失评分可重新获取信息补齐。
- 代理设置：输入框留空回车时自动填入默认代理 `http://127.0.0.1:7890`。
- 未下载列表空结果/失败状态显示明确提示与「重新获取」按钮。
- 移除设置中的「游戏识别严格度」板块。

### 修复

- 「选择目录后 Application error」：主组件内图标引用变量被遮蔽导致的运行时崩溃。
- 星标不显示：黄色样式类未编译进 CSS，改为内联样式 + 内联 SVG，确保任何环境可见。
- 移动端灰色滤镜失效：补全 `-webkit-filter` 前缀。
- 未下载封面半透明透出占位符问题。
- 会社空列表被永久缓存导致始终无法加载（自动重拉修复）。
- 页面更新无需强制刷新：JS 文件更换文件名 hash，浏览器自动加载新版本。
