# 第 3 批交付说明 · 详情弹窗 + 设置弹窗重构

> 工作副本：`E:\moeshelf-work`（原始目录 `E:\moeshelf-src` 始终只读，`git status` 干净）
> 预览地址：**http://localhost:3001**
> 前置：第 1 批（排版地基/卡片/滚动模型）、第 2 批（左侧导航/右栏/空态骨架）已完成

---

## 1. 详情弹窗：从「左空右挤」改成信息完整的两栏

### 改动前的问题（实测截图 `_recon/modal-detail.png`）
- 左栏封面按原始比例显示，**封面下方 400+px 全是空白**；
- 评分 / 厂商 / 发售日被挤到左栏最底部，与封面隔着大片黑；
- 主操作（启动、打开文件夹）**藏在「设置」面板里**，弹窗顶部只有「设置」和「关闭」两个圆钮；
- 弹窗内**两层滚动**（简介独立滚动框 + 角色横滑条）；
- 简介 14px 白 65%、角色名 11px，字小且灰。

### 改动后
| 项目 | 改动 |
|---|---|
| **顶部操作栏** | 标题 + 通关标记 + 原名（左），**启动 / 打开文件夹 / 重新获取 / 更多设置 / 关闭**（右）——主操作不再藏起来 |
| **左栏（sticky）** | 封面 + **评分 / 发售日 / 厂商 / 游玩时长 四张信息卡** + 所在目录 + 启动文件（可就地更换 exe），封面下方不再有空白 |
| **右栏** | 简介与「角色与声优」用**标签页切换**，整个弹窗内只剩 1 个滚动区（实测：弹窗内溢出滚动容器 = **0**） |
| **角色页** | 从横滑条改成**响应式网格**（3/4/6 列），角色立绘更大、名字 14px、声优 13px |
| **排版** | 标题 20px、简介用 `.prose-cn`（15px / 行高 1.8）、元数据统一 `panel-flat` 小卡 + 语义色令牌 |
| **尺寸** | `max-w-5xl` → `max-w-6xl`、`h-90vh` → `h-92vh` |

---

## 2. 设置（高级设置）弹窗：从单列表单改成左导航 + 右内容

### 改动前（`_recon/modal-settings.png`）
单列 640px、七项折叠面板竖着堆，每项都是「标题 + 说明 + 输入框 + 紫按钮」，说明文字 10-11px；「界面缩放」四个按钮是最显眼的控件却属于次要设置；没有导航、要点开才知道里面是什么。

### 改动后
| 项目 | 改动 |
|---|---|
| **结构** | 左侧分组导航（**基本信息 / 识别与匹配 / 运行 / 扩展数据**）+ 右侧当前分组内容，宽 4xl → 5xl；点导航即切换分组（单选手风琴，已展开的分组不会再被误收起） |
| **导航带图标** | 修改标题 / 更换封面 / 修正条目 / 厂商·制作组 / 修改路径 / 可执行文件 / 角色·声优，当前项高亮 |
| **标题栏** | 「高级设置 · 游戏名」，右上只留关闭；重复的「重新获取信息」「VNDB 跳转」已移到详情弹窗顶部，不再重复 |
| **控件统一** | 8 处输入框/下拉改用 `.field` 令牌、15 处按钮改用 `.btn btn-sm btn-primary / btn-soft / btn-ghost`、7 处面板容器改用 `.panel-flat`（原来硬编码 `bg-ink-800` 在亮色主题下是**深底深字**，现在两套主题都正常） |
| **文字规格** | 说明文字 10px/11px → **12px**，全部换成 `text-tertiary / text-quaternary / text-danger / text-success / text-warn` 语义色 |

---

## 3. 复验结果（`_recon/verify-batch3.mjs`）

| 验证项 | 结果 |
|---|---|
| 详情弹窗顶栏按钮 | `标记通关 · 启动 · 打开 · 更换 · 简介中文 · 角色与声优10` ✅ |
| 主操作在顶栏 | `hasLaunch = true`（启动按钮在顶部操作栏）✅ |
| 弹窗内滚动容器 | **0**（简介与角色只在右栏单一滚动区内）✅ |
| 角色标签页 | 点击后渲染 10 个角色卡（`assets/images` 11 张图请求）✅ |
| 高级设置分组 | 分组标题 `基本信息 / 识别与匹配 / 运行 / 扩展数据`，导航 7 项 ✅ |
| 分组切换 | 点「更换封面」→ 内容区切到封面面板；点「修正条目」→ 切到搜索面板 ✅ |
| 关闭链路 | 关闭设置 → 关闭详情 → 回到列表（dialogs=0，卡片 60）✅ |
| 生产构建 | `npm run build` → **Compiled successfully，exit 0** ✅ |
| 类型检查 | `npx tsc --noEmit` → 0 错误 ✅ |

---

## 4. 验收材料（`_recon/`）

| 文件 | 说明 |
|---|---|
| `modal-detail.png` / `modal-settings.png` | **改动前**的详情弹窗、设置弹窗（对照用） |
| `detail-v2-light.png` | 改动后的详情弹窗（简介页） |
| `verify3-detail-intro.png` | 详情弹窗 · 简介页（含顶部操作栏与左栏信息卡） |
| `verify3-detail-characters.png` | 详情弹窗 · 角色与声优页 |
| `verify3-manage-title.png` | 高级设置 · 修改标题 |
| `verify3-manage-cover.png` | 高级设置 · 更换封面 |
| `verify3-manage-fix.png` | 高级设置 · 修正条目 |

---

## 5. 下一步（第 4 批）

1. **移动端专门打磨**（你说先不管，排最后）：顶栏两行布局、筛选抽屉、卡片操作改 `…` 菜单、回到顶部；
2. **组件拆分**：`page.tsx` 当前约 5,000 行 → 拆到 `src/components/{shell,game,detail,settings,ui}`，主容器目标 < 600 行；
3. **构建与回归**：拆分后逐步复跑三批的验证脚本，确认行为不变；
4. 把三批的量测数据汇总成一份「改动前 / 改动后」总表放进 README 附录。
